ENV=local

# Optional: override full DB URL (e.g. SQLite for local dev)
# DATABASE_URL_OVERRIDE=sqlite:///./local.db

DB_HOST=localhost
DB_PORT=5432
DB_NAME=sentinelops
DB_USER=postgres
DB_PASSWORD=

STRIPE_API_KEY=
